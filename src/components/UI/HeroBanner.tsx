import React from 'react'

interface PropTypes {
  title: string
  description: string
  image?: string
}

const HeroBanner = ({ title, description }: PropTypes) => {
  return <div>HeroBanner</div>
}

export default HeroBanner
